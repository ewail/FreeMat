/*!
\page vtkwidgets_vtksplinewidget2 vtkSplineWidget2

<p>
Section: \ref sec_vtkwidgets "Visualization Toolkit Widget Classes"
\section Usage
 vtkSplineWidget2 is the vtkAbstractWidget subclass for
 vtkSplineRepresentation which manages the interactions with
 vtkSplineRepresentation. This is based on vtkSplineWidget.

To create an instance of class vtkSplineWidget2, simply
invoke its constructor as follows
\verbatim
  obj = vtkSplineWidget2
\endverbatim
\section Methods
The class vtkSplineWidget2 has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkSplineWidget2 class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkSplineWidget2 = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkSplineWidget2 = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>obj.SetRepresentation (vtkSplineRepresentation r)</tt> -  Create the default widget representation if one is not set. By default,
 this is an instance of the vtkSplineRepresentation class.
 </LI>
<LI>  <tt>obj.CreateDefaultRepresentation ()</tt> -  Create the default widget representation if one is not set. By default,
 this is an instance of the vtkSplineRepresentation class.
 </LI>
</UL>
*/
