/*!
\page inspection_maxdim MAXDIM Maximum Dimension in Array

<p>
Section: \ref sec_inspection "Inspection Functions"
\section Usage
The <tt>maxdim</tt> function returns the lowest order dimension
along which an array is largest.  The general syntax for its
use is
\verbatim
  n = maxdim(x)
\endverbatim
and is equivalent to min(find(size(x) == max(size(x)))).
*/
