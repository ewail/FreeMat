/*!
\page inspection_fieldnames FIELDNAMES Fieldnames of a Structure

<p>
Section: \ref sec_inspection "Inspection Functions"
\section Usage
Returns a cell array containing the names of the fields in
a structure array.  The syntax for its use is
\verbatim
   x = fieldnames(y)
\endverbatim
where <tt>y</tt> is a structure array of object array.  The result
is a cell array, with one entry per field in <tt>y</tt>.
\section Example
We define a simple structure array:

\if FRAGMENT
frag_inspection_fieldnames_000.m
0
y.foo = 3; y.goo = 'hello';
x = fieldnames(y)
\endif


\verbinclude frag_inspection_fieldnames_000.m.out 

*/
