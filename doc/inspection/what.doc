/*!
\page inspection_what WHAT List FreeMat Files In Directory

<p>
Section: \ref sec_inspection "Inspection Functions"
\section Usage
Lists files in a directory (or the current directory if no
argument is supplied) that are relevant to FreeMat.  These
are <tt>M</tt>-files, <tt>MAT</tt>-files, and class directories.  There
are several syntaxes for its use.  The first is
\verbatim
   what
\endverbatim
which lists the aforementioned items.  If you provide a 
path instead
\verbatim
   what path-to-folder
\endverbatim
then <tt>what</tt> will list the relevant FreeMat items in the specified
directory.
*/
