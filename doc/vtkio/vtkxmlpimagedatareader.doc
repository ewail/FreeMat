/*!
\page vtkio_vtkxmlpimagedatareader vtkXMLPImageDataReader

<p>
Section: \ref sec_vtkio "Visualization Toolkit IO Classes"
\section Usage
 vtkXMLPImageDataReader reads the PVTK XML ImageData file format.
 This reads the parallel format's summary file and then uses
 vtkXMLImageDataReader to read data from the individual ImageData
 piece files.  Streaming is supported.  The standard extension for
 this reader's file format is "pvti".

To create an instance of class vtkXMLPImageDataReader, simply
invoke its constructor as follows
\verbatim
  obj = vtkXMLPImageDataReader
\endverbatim
\section Methods
The class vtkXMLPImageDataReader has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkXMLPImageDataReader class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkXMLPImageDataReader = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkXMLPImageDataReader = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>vtkImageData = obj.GetOutput ()</tt> -  Get the reader's output.
 </LI>
<LI>  <tt>vtkImageData = obj.GetOutput (int idx)</tt> -  Get the reader's output.
 </LI>
<LI>  <tt>obj.CopyOutputInformation (vtkInformation outInfo, int port)</tt>
 </LI>
</UL>
*/
