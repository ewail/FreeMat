/*!
\page flow_warning WARNING Emits a Warning Message

<p>
Section: \ref sec_flow "Flow Control"
\section Usage
The <tt>warning</tt> function causes a warning message to be
sent to the user.  The general syntax for its use is
\verbatim
   warning(s)
\endverbatim
where <tt>s</tt> is the string message containing the warning.

The <tt>warning</tt> function can also be used to turn off warnings, and to
retrieve the current state of the warning flag.  To turn off warnings
use the syntax
\verbatim
   warning off
\endverbatim
at which point, warnings will not be displayed.  To turn on warnings
use the syntax
\verbatim
   warning on
\endverbatim
In both cases, you can also retrieve the current state of the warnings
flag
\verbatim
   y = warning('on')
   y = warning('off')
\endverbatim
*/
