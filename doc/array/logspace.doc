/*!
\page array_logspace LOGSPACE Logarithmically Spaced Vector

<p>
Section: \ref sec_array "Array Generation and Manipulations"
\section Usage
Generates a row vector with the specified number number of elements,
with entries logarithmically spaced between two specified endpoints.
The syntax for its use is either
\verbatim
    y = logspace(a,b,count)
\endverbatim
 or, for a default <tt>count = 50</tt>,
\verbatim
    y = logspace(a,b)
\endverbatim
 A third special use is when
\verbatim
    y = logspace(a,pi)
\endverbatim
where it generates points between <tt>10^a</tt> and <tt>pi</tt>

Contributed by Paulo Xavier Candeias under GPL.
\section Example
Here is an example of the use of <tt>logspace</tt>

\if FRAGMENT
frag_array_logspace_000.m
0
logspace(1,2,3)
\endif


\verbinclude frag_array_logspace_000.m.out 

*/
