/*!
\page array_flipud FLIPUD Reverse the Columns of a Matrix

<p>
Section: \ref sec_array "Array Generation and Manipulations"
\section USAGE
Reverses the rows of a matrix.  The syntax for its use is
\verbatim
   y = flipud(x)
\endverbatim
where <tt>x</tt> is matrix.  If <tt>x</tt> is an N-dimensional array then
the first dimension is reversed.
\section Example
The following example shows <tt>flipud</tt> applied to a 2D matrix.

\if FRAGMENT
frag_array_flipud_000.m
0
x = int32(rand(4)*10)
flipud(x)
\endif


\verbinclude frag_array_flipud_000.m.out 

For a 3D array, note how the rows in each slice are flipped.

\if FRAGMENT
frag_array_flipud_001.m
0
x = int32(rand(4,4,3)*10)
flipud(x)
\endif


\verbinclude frag_array_flipud_001.m.out 

*/
