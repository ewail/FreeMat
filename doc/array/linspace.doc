/*!
\page array_linspace LINSPACE Linearly Spaced Vector

<p>
Section: \ref sec_array "Array Generation and Manipulations"
\section Usage
Generates a row vector with the specified number of elements, with
entries uniformly spaced between two specified endpoints.  The syntax
for its use is either
\verbatim
   y = linspace(a,b,count)
\endverbatim
or, for a default <tt>count = 100</tt>,
\verbatim
   y = linspace(a,b);
\endverbatim
\section Examples
Here is a simple example of using <tt>linspace</tt>

\if FRAGMENT
frag_array_linspace_000.m
0
x = linspace(0,1,5)
\endif


\verbinclude frag_array_linspace_000.m.out 

*/
