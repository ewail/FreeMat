/*!
\page string_strtrim STRTRIM Trim Spaces from a String

<p>
Section: \ref sec_string "String Functions"
\section Usage
Removes the white-spaces at the beginning and end of a string (or a 
cell array of strings). See <tt>isspace</tt> for a definition of a white-space.
There are two forms for the <tt>strtrim</tt> function.  The first is for
single strings
\verbatim
   y = strtrim(strng)
\endverbatim
where <tt>strng</tt> is a string.  The second form operates on a cell array
of strings
\verbatim
   y = strtrim(cellstr)
\endverbatim
and trims each string in the cell array.
\section Example
Here we apply <tt>strtrim</tt> to a simple string

\if FRAGMENT
frag_string_strtrim_000.m
0
strtrim('  lot of blank spaces    ');
\endif


\verbinclude frag_string_strtrim_000.m.out 

and here we apply it to a cell array

\if FRAGMENT
frag_string_strtrim_001.m
0
strtrim({'  space','enough ',' for ',''})
\endif


\verbinclude frag_string_strtrim_001.m.out 

*/
