/*!
\page string_deblank DEBLANK Remove trailing blanks from a string

<p>
Section: \ref sec_string "String Functions"
\section Usage
The <tt>deblank</tt> function removes spaces at the end of a string
when used with the syntax
\verbatim
   y = deblank(x)
\endverbatim
where <tt>x</tt> is a string, in which case, all of the extra spaces
in <tt>x</tt> are stripped from the end of the string.  Alternately,
you can call <tt>deblank</tt> with a cell array of strings
\verbatim
   y = deblank(c)
\endverbatim
in which case each string in the cell array is deblanked.
\section Example
A simple example

\if FRAGMENT
frag_string_deblank_000.m
0
deblank('hello   ')
\endif


\verbinclude frag_string_deblank_000.m.out 

and a more complex example with a cell array of strings

\if FRAGMENT
frag_string_deblank_001.m
0
deblank({'hello  ','there ','  is  ','  sign  '})
\endif


\verbinclude frag_string_deblank_001.m.out 

*/
