/*!
\page string_cellstr CELLSTR Convert character array to cell array of strings

<p>
Section: \ref sec_string "String Functions"
\section Usage
The <tt>cellstr</tt> converts a character array matrix into a 
a cell array of individual strings.  Each string in
the matrix is placed in a different cell, and extra spaces
are removed.  The syntax for the command is
\verbatim
   y = cellstr(x)
\endverbatim
where <tt>x</tt> is an <tt>N x M</tt> array of characters as a string.
\section Example
Here is an example of how to use <tt>cellstr</tt>

\if FRAGMENT
frag_string_cellstr_000.m
0
a = ['quick';'brown';'fox  ';'is   ']
cellstr(a)
\endif


\verbinclude frag_string_cellstr_000.m.out 

*/
