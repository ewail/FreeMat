/*!
\page string_isspace ISSPACE Test for Space Characters in a String

<p>
Section: \ref sec_string "String Functions"
\section Usage
The <tt>isspace</tt> functions returns a logical array that is 1 
for characters in the argument string that are spaces, and 
is a logical 0 for characters in the argument that are not
spaces.  The syntax for its use is
\verbatim
   x = isspace(s)
\endverbatim
where <tt>s</tt> is a <tt>string</tt>.  A blank character is considered
a space, newline, tab, carriage return, formfeed, and vertical
tab.
\section Example
A simple example of <tt>isspace</tt>:

\if FRAGMENT
frag_string_isspace_000.m
0
isspace('  hello there world ')
\endif


\verbinclude frag_string_isspace_000.m.out 

*/
