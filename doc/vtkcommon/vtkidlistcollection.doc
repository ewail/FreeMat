/*!
\page vtkcommon_vtkidlistcollection vtkIdListCollection

<p>
Section: \ref sec_vtkcommon "Visualization Toolkit Common Classes"
\section Usage
 vtkIdListCollection is an object that creates and manipulates lists of
 datasets. See also vtkCollection and subclasses.

To create an instance of class vtkIdListCollection, simply
invoke its constructor as follows
\verbatim
  obj = vtkIdListCollection
\endverbatim
\section Methods
The class vtkIdListCollection has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkIdListCollection class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkIdListCollection = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkIdListCollection = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>obj.AddItem (vtkIdList ds)</tt> -  Get the next dataset in the list.
 </LI>
<LI>  <tt>vtkIdList = obj.GetNextItem ()</tt> -  Get the next dataset in the list.
 </LI>
<LI>  <tt>vtkIdList = obj.GetItem (int i)</tt> -  Get the ith dataset in the list.
 </LI>
</UL>
*/
