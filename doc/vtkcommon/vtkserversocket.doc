/*!
\page vtkcommon_vtkserversocket vtkServerSocket

<p>
Section: \ref sec_vtkcommon "Visualization Toolkit Common Classes"
\section Usage


To create an instance of class vtkServerSocket, simply
invoke its constructor as follows
\verbatim
  obj = vtkServerSocket
\endverbatim
\section Methods
The class vtkServerSocket has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkServerSocket class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkServerSocket = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkServerSocket = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>int = obj.CreateServer (int port)</tt> -  Creates a server socket at a given port and binds to it.
 Returns -1 on error. 0 on success.
 </LI>
<LI>  <tt>vtkClientSocket = obj.WaitForConnection (long msec)</tt> -  Waits for a connection. When a connection is received
 a new vtkClientSocket object is created and returned.
 Returns NULL on timeout. 
 </LI>
<LI>  <tt>int = obj.GetServerPort ()</tt> -  Returns the port on which the server is running.
 </LI>
</UL>
*/
