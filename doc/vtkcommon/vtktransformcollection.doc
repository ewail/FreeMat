/*!
\page vtkcommon_vtktransformcollection vtkTransformCollection

<p>
Section: \ref sec_vtkcommon "Visualization Toolkit Common Classes"
\section Usage
 vtkTransformCollection is an object that creates and manipulates lists of
 objects of type vtkTransform.

To create an instance of class vtkTransformCollection, simply
invoke its constructor as follows
\verbatim
  obj = vtkTransformCollection
\endverbatim
\section Methods
The class vtkTransformCollection has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkTransformCollection class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkTransformCollection = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkTransformCollection = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>obj.AddItem (vtkTransform )</tt> -  Add a Transform to the list.
 </LI>
<LI>  <tt>vtkTransform = obj.GetNextItem ()</tt> -  Get the next Transform in the list. Return NULL when the end of the
 list is reached.
 </LI>
</UL>
*/
