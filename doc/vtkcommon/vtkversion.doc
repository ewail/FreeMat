/*!
\page vtkcommon_vtkversion vtkVersion

<p>
Section: \ref sec_vtkcommon "Visualization Toolkit Common Classes"
\section Usage
 Holds methods for defining/determining the current vtk version
 (major, minor, build).

To create an instance of class vtkVersion, simply
invoke its constructor as follows
\verbatim
  obj = vtkVersion
\endverbatim
\section Methods
The class vtkVersion has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkVersion class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkVersion = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkVersion = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
</UL>
*/
