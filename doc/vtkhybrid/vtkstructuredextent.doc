/*!
\page vtkhybrid_vtkstructuredextent vtkStructuredExtent

<p>
Section: \ref sec_vtkhybrid "Visualization Toolkit Hybrid Classes"
\section Usage
  vtkStructuredExtent is an helper class that helps in arithmetic with
  structured extents. It defines a bunch of static methods (most of which are
  inlined) to aid in dealing with extents.

To create an instance of class vtkStructuredExtent, simply
invoke its constructor as follows
\verbatim
  obj = vtkStructuredExtent
\endverbatim
\section Methods
The class vtkStructuredExtent has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkStructuredExtent class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkStructuredExtent = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkStructuredExtent = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
</UL>
*/
