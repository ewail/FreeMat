/*!
\page glwin_glclump GLCLUMP Create a GL Clump

<p>
Section: \ref sec_glwin "OpenGL Models"
\section Usage
Defines an aggregate clump of objects that can be treated
as a node.  A GL Clump is defined by a vector consisting
of the following elements:
\verbatim
   [r1 g1 b1 n1 p1 p2 p3 ... r2 g2 b2 n2 p1 p2 p3 ... ]
\endverbatim
i.e., an RGB color spec, followed by a point count <tt>ni</tt>, followed
by a length <tt>ni</tt> vector of coordinates that are <tt>x,y,z</tt> triplets.
The usage of this function is
\verbatim
   glclump(name,vector)
\endverbatim
where <tt>name</tt> is the name of the clump and <tt>vector</tt> is the aforementioned
vector of points.
*/
