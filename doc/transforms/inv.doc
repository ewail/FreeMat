/*!
\page transforms_inv INV Invert Matrix

<p>
Section: \ref sec_transforms "Transforms/Decompositions"
\section Usage
Inverts the argument matrix, provided it is square and invertible.
The syntax for its use is
\verbatim
   y = inv(x)
\endverbatim
Internally, the <tt>inv</tt> function uses the matrix divide operators.
For sparse matrices, a sparse matrix solver is used.
\section Example
Here we invert some simple matrices

\if FRAGMENT
frag_transforms_inv_000.m
0
a = randi(zeros(3),5*ones(3))
b = inv(a)
a*b
b*a
\endif


\verbinclude frag_transforms_inv_000.m.out 

*/
