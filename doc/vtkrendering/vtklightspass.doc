/*!
\page vtkrendering_vtklightspass vtkLightsPass

<p>
Section: \ref sec_vtkrendering "Visualization Toolkit Rendering Classes"
\section Usage
 Render the lights.

 This pass expects an initialized camera.
 It disables all the lights, apply transformations for lights following the
 camera, and turn on the enables lights.
 

To create an instance of class vtkLightsPass, simply
invoke its constructor as follows
\verbatim
  obj = vtkLightsPass
\endverbatim
\section Methods
The class vtkLightsPass has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkLightsPass class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkLightsPass = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkLightsPass = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
</UL>
*/
