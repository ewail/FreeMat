/*!
\page external_ctypeprint CTYPEPRINT Print C Type

<p>
Section: \ref sec_external "FreeMat External Interface"
\section Usage
The <tt>ctypeprint</tt> function prints a C type on the console.  The 
usage is
\verbatim
  ctypeprint(typename)
\endverbatim
where <tt>typename</tt> is a string containing the name of the C type to print.
Depending on the class of the C type (e.g., structure, alias or enumeration)
the <tt>ctypeprint</tt> function will dump information about the type definition.
*/
