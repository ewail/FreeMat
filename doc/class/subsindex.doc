/*!
\page class_subsindex SUBSINDEX Overloaded Class Indexing

<p>
Section: \ref sec_class "Object Oriented Programming"
\section Usage
This method is called for classes in the expressions
of the form
\verbatim
  c = subsindex(a)
\endverbatim
where <tt>a</tt> is an object, and <tt>c</tt> is an index vector.
It is also called for
\verbatim
  c = b(a)
\endverbatim
in which case <tt>subsindex(a)</tt> must return a vector containing
integers between <tt>0</tt> and <tt>N-1</tt> where <tt>N</tt> is the number
of elements in the vector <tt>b</tt>.
*/
