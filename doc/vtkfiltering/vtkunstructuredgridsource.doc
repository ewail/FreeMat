/*!
\page vtkfiltering_vtkunstructuredgridsource vtkUnstructuredGridSource

<p>
Section: \ref sec_vtkfiltering "Visualization Toolkit Filtering Classes"
\section Usage
 vtkUnstructuredGridSource is an abstract class whose subclasses generate 
 unstructured grid data.

To create an instance of class vtkUnstructuredGridSource, simply
invoke its constructor as follows
\verbatim
  obj = vtkUnstructuredGridSource
\endverbatim
\section Methods
The class vtkUnstructuredGridSource has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkUnstructuredGridSource class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkUnstructuredGridSource = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkUnstructuredGridSource = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>vtkUnstructuredGrid = obj.GetOutput ()</tt> -  Get the output of this source.
 </LI>
<LI>  <tt>vtkUnstructuredGrid = obj.GetOutput (int idx)</tt> -  Get the output of this source.
 </LI>
<LI>  <tt>obj.SetOutput (vtkUnstructuredGrid output)</tt> -  Get the output of this source.
 </LI>
</UL>
*/
