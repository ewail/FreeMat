/*!
\page vtkfiltering_vtkcachedstreamingdemanddrivenpipeline vtkCachedStreamingDemandDrivenPipeline

<p>
Section: \ref sec_vtkfiltering "Visualization Toolkit Filtering Classes"
\section Usage
 vtkCachedStreamingDemandDrivenPipeline

To create an instance of class vtkCachedStreamingDemandDrivenPipeline, simply
invoke its constructor as follows
\verbatim
  obj = vtkCachedStreamingDemandDrivenPipeline
\endverbatim
\section Methods
The class vtkCachedStreamingDemandDrivenPipeline has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkCachedStreamingDemandDrivenPipeline class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkCachedStreamingDemandDrivenPipeline = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkCachedStreamingDemandDrivenPipeline = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>int = obj.Update ()</tt> -  Bring the algorithm's outputs up-to-date.
 </LI>
<LI>  <tt>int = obj.Update (int port)</tt> -  Bring the algorithm's outputs up-to-date.
 </LI>
<LI>  <tt>obj.SetCacheSize (int size)</tt> -  This is the maximum number of images that can be retained in memory.
 it defaults to 10.
 </LI>
<LI>  <tt>int = obj.GetCacheSize ()</tt> -  This is the maximum number of images that can be retained in memory.
 it defaults to 10.
 </LI>
</UL>
*/
