/*!
\page vtkfiltering_vtkstructuredpointstopolydatafilter vtkStructuredPointsToPolyDataFilter

<p>
Section: \ref sec_vtkfiltering "Visualization Toolkit Filtering Classes"
\section Usage
 vtkStructuredPointsToPolyDataFilter is an abstract filter class whose
 subclasses take on input structured points and generate polygonal 
 data on output.

To create an instance of class vtkStructuredPointsToPolyDataFilter, simply
invoke its constructor as follows
\verbatim
  obj = vtkStructuredPointsToPolyDataFilter
\endverbatim
\section Methods
The class vtkStructuredPointsToPolyDataFilter has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkStructuredPointsToPolyDataFilter class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkStructuredPointsToPolyDataFilter = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkStructuredPointsToPolyDataFilter = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>obj.SetInput (vtkImageData input)</tt> -  Set / get the input data or filter.
 </LI>
<LI>  <tt>vtkImageData = obj.GetInput ()</tt> -  Set / get the input data or filter.
 </LI>
</UL>
*/
