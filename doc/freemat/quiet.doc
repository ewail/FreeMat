/*!
\page freemat_quiet QUIET Control the Verbosity of the Interpreter

<p>
Section: \ref sec_freemat "FreeMat Functions"
\section Usage
The <tt>quiet</tt> function controls how verbose the interpreter
is when executing code.  The syntax for the function is
\verbatim
   quiet flag
\endverbatim
where <tt>flag</tt> is one of
<UL>
<LI>  <tt>'normal'</tt> - normal output from the interpreter
 </LI>
<LI>  <tt>'quiet'</tt> - only intentional output (e.g. <tt>printf</tt> calls and
<tt>disp</tt> calls) is printed.  The output of expressions that are not
terminated in semicolons are not printed.
 </LI>
<LI>  <tt>'silent'</tt> - nothing is printed to the output.
 </LI>
</UL>
The <tt>quiet</tt> command also returns the current quiet flag.
*/
