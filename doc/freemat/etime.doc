/*!
\page freemat_etime ETIME Elapsed Time Function

<p>
Section: \ref sec_freemat "FreeMat Functions"
\section Usage
The <tt>etime</tt> calculates the elapsed time between two <tt>clock</tt> vectors
<tt>x1</tt> and <tt>x2</tt>.  The syntax for its use is
\verbatim
   y = etime(x1,x2)
\endverbatim
where <tt>x1</tt> and <tt>x2</tt> are in the <tt>clock</tt> output format
\verbatim
   x = [year month day hour minute seconds]
\endverbatim
\section Example
Here we use <tt>etime</tt> as a substitute for <tt>tic</tt> and <tt>toc</tt>

\if FRAGMENT
frag_freemat_etime_000.m
0
x1 = clock;
sleep(1);
x2 = clock;
etime(x2,x1);
\endif


\verbinclude frag_freemat_etime_000.m.out 

*/
