/*!
\page freemat_startup STARTUP Startup Script

<p>
Section: \ref sec_freemat "FreeMat Functions"
\section Usage
Upon starting, FreeMat searches for a script names <tt>startup.m</tt>, and
if it finds it, it executes it.  This script can be in the current
directory, or on the FreeMat path (set using <tt>setpath</tt>).  The contents
of startup.m must be a valid script (not a function).  
*/
