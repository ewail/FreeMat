/*!
\page vtkimaging_vtkimagepadfilter vtkImagePadFilter

<p>
Section: \ref sec_vtkimaging "Visualization Toolkit Imaging Classes"
\section Usage
 vtkImagePadFilter Changes the image extent of an image.  If the image
 extent is larger than the input image extent, the extra pixels are
 filled by an algorithm determined by the subclass.
 The image extent of the output has to be specified.

To create an instance of class vtkImagePadFilter, simply
invoke its constructor as follows
\verbatim
  obj = vtkImagePadFilter
\endverbatim
\section Methods
The class vtkImagePadFilter has several methods that can be used.
  They are listed below.
Note that the documentation is translated automatically from the VTK sources,
and may not be completely intelligible.  When in doubt, consult the VTK website.
In the methods listed below, <tt>obj</tt> is an instance of the vtkImagePadFilter class.
<UL>
<LI>  <tt>string = obj.GetClassName ()</tt>
 </LI>
<LI>  <tt>int = obj.IsA (string name)</tt>
 </LI>
<LI>  <tt>vtkImagePadFilter = obj.NewInstance ()</tt>
 </LI>
<LI>  <tt>vtkImagePadFilter = obj.SafeDownCast (vtkObject o)</tt>
 </LI>
<LI>  <tt>obj.SetOutputWholeExtent (int extent[6])</tt> -  The image extent of the output has to be set explicitly.
 </LI>
<LI>  <tt>obj.SetOutputWholeExtent (int minX, int maxX, int minY, int maxY, int minZ, int maxZ)</tt> -  The image extent of the output has to be set explicitly.
 </LI>
<LI>  <tt>obj.GetOutputWholeExtent (int extent[6])</tt> -  The image extent of the output has to be set explicitly.
 </LI>
<LI>  <tt>int = obj.GetOutputWholeExtent ()</tt> -  Set/Get the number of output scalar components.
 </LI>
<LI>  <tt>obj.SetOutputNumberOfScalarComponents (int )</tt> -  Set/Get the number of output scalar components.
 </LI>
<LI>  <tt>int = obj.GetOutputNumberOfScalarComponents ()</tt> -  Set/Get the number of output scalar components.
 </LI>
</UL>
*/
