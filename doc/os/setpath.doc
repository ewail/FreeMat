/*!
\page os_setpath SETPATH Set Current Search Path

<p>
Section: \ref sec_os "Operating System Functions"
\section Usage
Changes the current FreeMat search path.  The general syntax for
its use is
\verbatim
  setpath(y)
\endverbatim
where <tt>y</tt> is a <tt>string</tt> containing a delimited list of directories
to be searched for M files and libraries.  
The delimiter between the paths depends on the system being used.  For Win32, the
delimiter is a semicolon.  For all other systems, the delimiter is a colon.

\section Example
The <tt>setpath</tt> function is straightforward.

\if FRAGMENT
frag_os_setpath_000.m
0
getpath
setpath('/usr/local/FreeMat/MFiles:/localhome/basu/MFiles')
getpath
\endif


\verbinclude frag_os_setpath_000.m.out 

*/
