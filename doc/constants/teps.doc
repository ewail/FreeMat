/*!
\page constants_teps TEPS Type-based Epsilon Calculation

<p>
Section: \ref sec_constants "Base Constants"
\section Usage
Returns <tt>eps</tt> for double precision arguments and
<tt>feps</tt> for single precision arguments.  The syntax for
<tt>teps</tt> is
\verbatim
   y = teps(x)
\endverbatim
The <tt>teps</tt> function is most useful if you need to
compute epsilon based on the type of the array.
\section Example
The following example demonstrates the use of the <tt>teps</tt> function,
and one of its numerical consequences.

\if FRAGMENT
frag_constants_teps_000.m
0
teps(float(3.4))
teps(complex(3.4+i*2))
\endif


\verbinclude frag_constants_teps_000.m.out 

*/
