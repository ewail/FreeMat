<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="handle_zplane" kind="page">
    <compoundname>handle_zplane</compoundname>
    <title>ZPLANE Zero-pole plot</title>
    <detaileddescription>
<para>Section: <ref refid="sec_handle_1sec_handle" kindref="member">Handle-Based Graphics</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>This function makes a zero-pole plot of a discrete-time system defined by its zeros and poles. The various syntaxes are <verbatim>    zplane(z,p)
</verbatim> where <computeroutput>z</computeroutput> and <computeroutput>p</computeroutput> are the zeros and the poles of the system stored as column vectors, or <verbatim>    zplane(b,a)
</verbatim> where <computeroutput>a</computeroutput> and <computeroutput>b</computeroutput> are the polynomial coefficients of the numerator and denominator stored as line vectors (<computeroutput>roots</computeroutput> is used to find the zeros and poles). The symbol <computeroutput>&apos;o&apos;</computeroutput> represents a zero and the symbol <computeroutput>&apos;x&apos;</computeroutput> represents a pole. The plot includes the unit circle for reference. Contributed by Paulo Xavier Candeias under GPL </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
