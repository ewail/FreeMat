<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="sparse_sparse" kind="page">
    <compoundname>sparse_sparse</compoundname>
    <title>SPARSE Construct a Sparse Matrix</title>
    <detaileddescription>
<para>Section: <ref refid="sec_sparse_1sec_sparse" kindref="member">Sparse Matrix Support</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Creates a sparse matrix using one of several formats. The first creates a sparse matrix from a full matrix <verbatim>   y = sparse(x).
</verbatim> The second form creates a sparse matrix containing all zeros that is of the specified size (the sparse equivalent of <computeroutput>zeros</computeroutput>). <verbatim>   y = sparse(m,n)
</verbatim> where <computeroutput>m</computeroutput> and <computeroutput>n</computeroutput> are integers. Just like the <computeroutput>zeros</computeroutput> function, the sparse matrix returned is of type <computeroutput>float</computeroutput>. The third form constructs a sparse matrix from the IJV syntax. It has two forms. The first version autosizes the sparse matrix <verbatim>   y = sparse(i,j,v)
</verbatim> while the second version uses an explicit size specification <verbatim>   y = sparse(i,j,v,m,n)
</verbatim> </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
