<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="os_mkdir" kind="page">
    <compoundname>os_mkdir</compoundname>
    <title>MKDIR Make Directory</title>
    <detaileddescription>
<para>Section: <ref refid="sec_os_1sec_os" kindref="member">Operating System Functions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Creates a directory. The general syntax for its use is <verbatim>  mkdir(&apos;dirname&apos;)
</verbatim> which creates the directory <computeroutput>dirname</computeroutput> if it does not exist. The argument <computeroutput>dirname</computeroutput> can be either a relative path or an absolute path. For compatibility with MATLAB, the following syntax is also allowed <verbatim>  mkdir(&apos;parentdir&apos;,&apos;dirname&apos;)
</verbatim> which attempts to create a directory <computeroutput>dirname</computeroutput> in the directory given by <computeroutput>parentdir</computeroutput>. However, this simply calls <computeroutput>mkdir([parentdir dirsep dirname])</computeroutput>, and if this is not the required behavior, please file an enhancement request to have it changed. Note that <computeroutput>mkdir</computeroutput> returns a logical <computeroutput>1</computeroutput> if the call succeeded, and a logical <computeroutput>0</computeroutput> if not. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
