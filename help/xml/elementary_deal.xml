<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="elementary_deal" kind="page">
    <compoundname>elementary_deal</compoundname>
    <title>DEAL Multiple Simultaneous Assignments</title>
    <detaileddescription>
<para>Section: <ref refid="sec_elementary_1sec_elementary" kindref="member">Elementary Functions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>When making a function call, it is possible to assign multiple outputs in a single call, (see, e.g., <computeroutput>max</computeroutput> for an example). The <computeroutput>deal</computeroutput> call allows you to do the same thing with a simple assignment. The syntax for its use is <verbatim>   [a,b,c,...] = deal(expr)
</verbatim> where <computeroutput>expr</computeroutput> is an expression with multiple values. The simplest example is where <computeroutput>expr</computeroutput> is the dereference of a cell array, e.g. <computeroutput>expr &lt;<ndash/> A{:}</computeroutput>. In this case, the <computeroutput>deal</computeroutput> call is equivalent to <verbatim>   a = A{1}; b = A{2}; C = A{3}; 
</verbatim> Other expressions which are multivalued are structure arrays with multiple entries (non-scalar), where field dereferencing has been applied. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
