<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="transforms_fftn" kind="page">
    <compoundname>transforms_fftn</compoundname>
    <title>FFTN N-Dimensional Forward FFT</title>
    <detaileddescription>
<para>Section: <ref refid="sec_transforms_1sec_transforms" kindref="member">Transforms/Decompositions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Computes the DFT of an N-dimensional numerical array along all dimensions. The general syntax for its use is <verbatim>  y = fftn(x)
</verbatim> which computes the same-size FFTs for each dimension of <computeroutput>x</computeroutput>. Alternately, you can specify the size vector <verbatim>  y = fftn(x,dims)
</verbatim> where <computeroutput>dims</computeroutput> is a vector of sizes. The array <computeroutput>x</computeroutput> is zero padded or truncated as necessary in each dimension so that the output is of size <computeroutput>dims</computeroutput>. The <computeroutput>fftn</computeroutput> function is implemented by a sequence of calls to <computeroutput>fft</computeroutput>. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
