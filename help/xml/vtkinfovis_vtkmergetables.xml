<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="vtkinfovis_vtkmergetables" kind="page">
    <compoundname>vtkinfovis_vtkmergetables</compoundname>
    <title>vtkMergeTables</title>
    <detaileddescription>
<para>Section: <ref refid="sec_vtkinfovis_1sec_vtkinfovis" kindref="member">Visualization Toolkit Infovis Classes</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Combines the columns of two tables into one larger table. The number of rows in the resulting table is the sum of the number of rows in each of the input tables. The number of columns in the output is generally the sum of the number of columns in each input table, except in the case where column names are duplicated in both tables. In this case, if MergeColumnsByName is on (the default), the two columns will be merged into a single column of the same name. If MergeColumnsByName is off, both columns will exist in the output. You may set the FirstTablePrefix and SecondTablePrefix to define how the columns named are modified. One of these prefixes may be the empty string, but they must be different.</para><para>To create an instance of class vtkMergeTables, simply invoke its constructor as follows <verbatim>  obj = vtkMergeTables
</verbatim> </para></sect1>
<sect1 id="vtkwidgets_vtkxyplotwidget_1Methods">
<title>Methods</title>
<para>The class vtkMergeTables has several methods that can be used. They are listed below. Note that the documentation is translated automatically from the VTK sources, and may not be completely intelligible. When in doubt, consult the VTK website. In the methods listed below, <computeroutput>obj</computeroutput> is an instance of the vtkMergeTables class. <itemizedlist>
<listitem>
<para><computeroutput>string = obj.GetClassName ()</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>int = obj.IsA (string name)</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>vtkMergeTables = obj.NewInstance ()</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>vtkMergeTables = obj.SafeDownCast (vtkObject o)</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>obj.SetFirstTablePrefix (string )</computeroutput> - The prefix to give to same-named fields from the first table. Default is &quot;Table1.&quot;.  </para></listitem>
<listitem>
<para><computeroutput>string = obj.GetFirstTablePrefix ()</computeroutput> - The prefix to give to same-named fields from the first table. Default is &quot;Table1.&quot;.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetSecondTablePrefix (string )</computeroutput> - The prefix to give to same-named fields from the second table. Default is &quot;Table2.&quot;.  </para></listitem>
<listitem>
<para><computeroutput>string = obj.GetSecondTablePrefix ()</computeroutput> - The prefix to give to same-named fields from the second table. Default is &quot;Table2.&quot;.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMergeColumnsByName (bool )</computeroutput> - If on, merges columns with the same name. If off, keeps both columns, but calls one FirstTablePrefix + name, and the other SecondTablePrefix + name. Default is on.  </para></listitem>
<listitem>
<para><computeroutput>bool = obj.GetMergeColumnsByName ()</computeroutput> - If on, merges columns with the same name. If off, keeps both columns, but calls one FirstTablePrefix + name, and the other SecondTablePrefix + name. Default is on.  </para></listitem>
<listitem>
<para><computeroutput>obj.MergeColumnsByNameOn ()</computeroutput> - If on, merges columns with the same name. If off, keeps both columns, but calls one FirstTablePrefix + name, and the other SecondTablePrefix + name. Default is on.  </para></listitem>
<listitem>
<para><computeroutput>obj.MergeColumnsByNameOff ()</computeroutput> - If on, merges columns with the same name. If off, keeps both columns, but calls one FirstTablePrefix + name, and the other SecondTablePrefix + name. Default is on.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetPrefixAllButMerged (bool )</computeroutput> - If on, all columns will have prefixes except merged columns. If off, only unmerged columns with the same name will have prefixes. Default is off.  </para></listitem>
<listitem>
<para><computeroutput>bool = obj.GetPrefixAllButMerged ()</computeroutput> - If on, all columns will have prefixes except merged columns. If off, only unmerged columns with the same name will have prefixes. Default is off.  </para></listitem>
<listitem>
<para><computeroutput>obj.PrefixAllButMergedOn ()</computeroutput> - If on, all columns will have prefixes except merged columns. If off, only unmerged columns with the same name will have prefixes. Default is off.  </para></listitem>
<listitem>
<para><computeroutput>obj.PrefixAllButMergedOff ()</computeroutput> - If on, all columns will have prefixes except merged columns. If off, only unmerged columns with the same name will have prefixes. Default is off.  </para></listitem>
</itemizedlist>
</para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
