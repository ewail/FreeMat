<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="vtkimaging_vtkimagemask" kind="page">
    <compoundname>vtkimaging_vtkimagemask</compoundname>
    <title>vtkImageMask</title>
    <detaileddescription>
<para>Section: <ref refid="sec_vtkimaging_1sec_vtkimaging" kindref="member">Visualization Toolkit Imaging Classes</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>vtkImageMask combines a mask with an image. Non zero mask implies the output pixel will be the same as the image. If a mask pixel is zero, then the output pixel is set to &quot;MaskedValue&quot;. The filter also has the option to pass the mask through a boolean not operation before processing the image. This reverses the passed and replaced pixels. The two inputs should have the same &quot;WholeExtent&quot;. The mask input should be unsigned char, and the image scalar type is the same as the output scalar type.</para><para>To create an instance of class vtkImageMask, simply invoke its constructor as follows <verbatim>  obj = vtkImageMask
</verbatim> </para></sect1>
<sect1 id="vtkwidgets_vtkxyplotwidget_1Methods">
<title>Methods</title>
<para>The class vtkImageMask has several methods that can be used. They are listed below. Note that the documentation is translated automatically from the VTK sources, and may not be completely intelligible. When in doubt, consult the VTK website. In the methods listed below, <computeroutput>obj</computeroutput> is an instance of the vtkImageMask class. <itemizedlist>
<listitem>
<para><computeroutput>string = obj.GetClassName ()</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>int = obj.IsA (string name)</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>vtkImageMask = obj.NewInstance ()</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>vtkImageMask = obj.SafeDownCast (vtkObject o)</computeroutput>  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMaskedOutputValue (int num, double v)</computeroutput> - SetGet the value of the output pixel replaced by mask.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMaskedOutputValue (double v)</computeroutput> - SetGet the value of the output pixel replaced by mask.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMaskedOutputValue (double v1, double v2)</computeroutput> - SetGet the value of the output pixel replaced by mask.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMaskedOutputValue (double v1, double v2, double v3)</computeroutput> - SetGet the value of the output pixel replaced by mask.  </para></listitem>
<listitem>
<para><computeroutput>int = obj.GetMaskedOutputValueLength ()</computeroutput> - Set/Get the alpha blending value for the mask The input image is assumed to be at alpha = 1.0 and the mask image uses this alpha to blend using an over operator.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMaskAlpha (double )</computeroutput> - Set/Get the alpha blending value for the mask The input image is assumed to be at alpha = 1.0 and the mask image uses this alpha to blend using an over operator.  </para></listitem>
<listitem>
<para><computeroutput>double = obj.GetMaskAlphaMinValue ()</computeroutput> - Set/Get the alpha blending value for the mask The input image is assumed to be at alpha = 1.0 and the mask image uses this alpha to blend using an over operator.  </para></listitem>
<listitem>
<para><computeroutput>double = obj.GetMaskAlphaMaxValue ()</computeroutput> - Set/Get the alpha blending value for the mask The input image is assumed to be at alpha = 1.0 and the mask image uses this alpha to blend using an over operator.  </para></listitem>
<listitem>
<para><computeroutput>double = obj.GetMaskAlpha ()</computeroutput> - Set/Get the alpha blending value for the mask The input image is assumed to be at alpha = 1.0 and the mask image uses this alpha to blend using an over operator.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetImageInput (vtkImageData in)</computeroutput> - Set the input to be masked.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetMaskInput (vtkImageData in)</computeroutput> - Set the mask to be used.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetNotMask (int )</computeroutput> - When Not Mask is on, the mask is passed through a boolean not before it is used to mask the image. The effect is to pass the pixels where the input mask is zero, and replace the pixels where the input value is non zero.  </para></listitem>
<listitem>
<para><computeroutput>int = obj.GetNotMask ()</computeroutput> - When Not Mask is on, the mask is passed through a boolean not before it is used to mask the image. The effect is to pass the pixels where the input mask is zero, and replace the pixels where the input value is non zero.  </para></listitem>
<listitem>
<para><computeroutput>obj.NotMaskOn ()</computeroutput> - When Not Mask is on, the mask is passed through a boolean not before it is used to mask the image. The effect is to pass the pixels where the input mask is zero, and replace the pixels where the input value is non zero.  </para></listitem>
<listitem>
<para><computeroutput>obj.NotMaskOff ()</computeroutput> - When Not Mask is on, the mask is passed through a boolean not before it is used to mask the image. The effect is to pass the pixels where the input mask is zero, and replace the pixels where the input value is non zero.  </para></listitem>
<listitem>
<para><computeroutput>obj.SetInput1 (vtkDataObject in)</computeroutput> - Set the two inputs to this filter  </para></listitem>
<listitem>
<para><computeroutput>obj.SetInput2 (vtkDataObject in)</computeroutput>  </para></listitem>
</itemizedlist>
</para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
