<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="array_num2str" kind="page">
    <compoundname>array_num2str</compoundname>
    <title>NUM2STR Convert Numbers To Strings</title>
    <detaileddescription>
<para>Section: <ref refid="sec_array_1sec_array" kindref="member">Array Generation and Manipulations</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Converts an array into its string representation. The general syntax for this function is <verbatim>   s = num2str(X)
</verbatim> where <computeroutput>s</computeroutput> is a string (or string matrix) and <computeroutput>X</computeroutput> is an array. By default, the <computeroutput>num2str</computeroutput> function uses 4 digits of precision and an exponent if required. If you want more digits of precision, you can specify the precition via the form <verbatim>   s = num2str(X, precision)
</verbatim> where <computeroutput>precision</computeroutput> is the number of digits to include in the string representation. For more control over the format of the output, you can also specify a format specifier (see <computeroutput>printf</computeroutput> for more details). <verbatim>   s = num2str(X, format)
</verbatim> where <computeroutput>format</computeroutput> is the specifier string. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
