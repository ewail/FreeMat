<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="external_loadlib" kind="page">
    <compoundname>external_loadlib</compoundname>
    <title>LOADLIB Load Library Function</title>
    <detaileddescription>
<para>Section: <ref refid="sec_external_1sec_external" kindref="member">FreeMat External Interface</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>The <computeroutput>loadlib</computeroutput> function allows a function in an external library to be added to FreeMat dynamically. This interface is generally to be used as last resort, as the form of the function being called is assumed to match the internal implementation. In short, this is not the interface mechanism of choice. For all but very complicated functions, the <computeroutput>import</computeroutput> function is the preferred approach. Thus, only a very brief summary of it is presented here. The syntax for <computeroutput>loadlib</computeroutput> is <verbatim>  loadlib(libfile, symbolname, functionname, nargin, nargout)
</verbatim> where <computeroutput>libfile</computeroutput> is the complete path to the library to use, <computeroutput>symbolname</computeroutput> is the name of the symbol in the library, <computeroutput>functionname</computeroutput> is the name of the function after it is imported into FreeMat (this is optional, it defaults to the <computeroutput>symbolname</computeroutput>), <computeroutput>nargin</computeroutput> is the number of input arguments (defaults to 0), and <computeroutput>nargout</computeroutput> is the number of output arguments (defaults to 0). If the number of (input or output) arguments is variable then set the corresponding argument to <computeroutput>-1</computeroutput>. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
