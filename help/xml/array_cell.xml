<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="array_cell" kind="page">
    <compoundname>array_cell</compoundname>
    <title>CELL Cell Array of Empty Matrices</title>
    <detaileddescription>
<para>Section: <ref refid="sec_array_1sec_array" kindref="member">Array Generation and Manipulations</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Creates a cell array of empty matrix entres. Two seperate syntaxes are possible. The first syntax specifies the array dimensions as a sequence of scalar dimensions: <verbatim>   y = cell(d1,d2,...,dn).
</verbatim> The resulting array has the given dimensions, and is filled with all zeros. The type of <computeroutput>y</computeroutput> is <computeroutput>cell</computeroutput>, a cell array.</para><para>The second syntax specifies the array dimensions as a vector, where each element in the vector specifies a dimension length: <verbatim>   y = cell([d1,d2,...,dn]).
</verbatim> This syntax is more convenient for calling <computeroutput>zeros</computeroutput> using a variable for the argument. In both cases, specifying only one dimension results in a square matrix output. </para></sect1>
<sect1 id="variables_struct_1Example">
<title>Example</title>
<para>The following examples demonstrate generation of some zero arrays using the first form.</para><para><verbatim>--&gt; cell(2,3,2)

ans = 

(:,:,1) = 
 [] [] [] 
 [] [] [] 

(:,:,2) = 
 [] [] [] 
 [] [] [] 

--&gt; cell(1,3)

ans = 
 [] [] [] 
</verbatim></para><para>The same expressions, using the second form.</para><para><verbatim>--&gt; cell([2,6])

ans = 
 [] [] [] [] [] [] 
 [] [] [] [] [] [] 

--&gt; cell([1,3])

ans = 
 [] [] [] 
</verbatim> </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
