<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="handle_axes" kind="page">
    <compoundname>handle_axes</compoundname>
    <title>AXES Create Handle Axes</title>
    <detaileddescription>
<para>Section: <ref refid="sec_handle_1sec_handle" kindref="member">Handle-Based Graphics</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>This function has three different syntaxes. The first takes no arguments, <verbatim>  h = axes
</verbatim> and creates a new set of axes that are parented to the current figure (see <computeroutput>gcf</computeroutput>). The newly created axes are made the current axes (see <computeroutput>gca</computeroutput>) and are added to the end of the list of children for the current figure. The second form takes a set of property names and values <verbatim>  h = axes(propertyname,value,propertyname,value,...)
</verbatim> Creates a new set of axes, and then sets the specified properties to the given value. This is a shortcut for calling <computeroutput>set(h,propertyname,value)</computeroutput> for each pair. The third form takes a handle as an argument <verbatim>  axes(handle)
</verbatim> and makes <computeroutput>handle</computeroutput> the current axes, placing it at the head of the list of children for the current figure. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
