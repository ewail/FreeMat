<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="io_wavplay" kind="page">
    <compoundname>io_wavplay</compoundname>
    <title>WAVPLAY</title>
    <detaileddescription>
<para>Section: <ref refid="sec_io_1sec_io" kindref="member">Input/Ouput Functions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Plays a linear PCM set of samples through the audio system. This function is only available if the <computeroutput>portaudio</computeroutput> library was available when FreeMat was built. The syntax for the command is one of: <verbatim>   wavplay(y)
   wavplay(y,sampling_rate)
   wavplay(...,mode)
</verbatim> where <computeroutput>y</computeroutput> is a matrix of audio samples. If <computeroutput>y</computeroutput> has two columns, then the audio playback is in stereo. The <computeroutput>y</computeroutput> input can be of types <computeroutput>float, double, int32, int16, int8, uint8</computeroutput>. For <computeroutput>float</computeroutput> and <computeroutput>double</computeroutput> types, the sample values in <computeroutput>y</computeroutput> must be between <computeroutput>-1</computeroutput> and <computeroutput>1</computeroutput>. The <computeroutput>sampling_rate</computeroutput> specifies the rate at which the data is recorded. If not specified, the <computeroutput>sampling_rate</computeroutput> defaults to <computeroutput>11025Hz</computeroutput>. Finally, you can specify a playback mode of <computeroutput>&apos;sync&apos;</computeroutput> which is synchronous playback or a playback mode of <computeroutput>&apos;async&apos;</computeroutput> which is asynchronous playback. For <computeroutput>&apos;sync&apos;</computeroutput> playback, the wavplay function returns when the playback is complete. For <computeroutput>&apos;async&apos;</computeroutput> playback, the function returns immediately (unless a former playback is still issuing). </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
