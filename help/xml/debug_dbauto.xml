<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="debug_dbauto" kind="page">
    <compoundname>debug_dbauto</compoundname>
    <title>DBAUTO Control Dbauto Functionality</title>
    <detaileddescription>
<para>Section: <ref refid="sec_debug_1sec_debug" kindref="member">Debugging FreeMat Code</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>The dbauto functionality in FreeMat allows you to debug your FreeMat programs. When <computeroutput>dbauto</computeroutput> is <computeroutput>on</computeroutput>, then any error that occurs while the program is running causes FreeMat to stop execution at that point and return you to the command line (just as if you had placed a <computeroutput>keyboard</computeroutput> command there). You can then examine variables, modify them, and resume execution using <computeroutput>return</computeroutput>. Alternately, you can exit out of all running routines via a <computeroutput>retall</computeroutput> statement. Note that errors that occur inside of <computeroutput>try</computeroutput>/<computeroutput>catch</computeroutput> blocks do not (by design) cause auto breakpoints. The <computeroutput>dbauto</computeroutput> function toggles the dbauto state of FreeMat. The syntax for its use is <verbatim>   dbauto(state)
</verbatim> where <computeroutput>state</computeroutput> is either <verbatim>   dbauto(&apos;on&apos;)
</verbatim> to activate dbauto, or <verbatim>   dbauto(&apos;off&apos;)
</verbatim> to deactivate dbauto. Alternately, you can use FreeMat&apos;s string-syntax equivalence and enter <verbatim>   dbauto on
</verbatim> or <verbatim>   dbauto off
</verbatim> to turn dbauto on or off (respectively). Entering <computeroutput>dbauto</computeroutput> with no arguments returns the current state (either &apos;on&apos; or &apos;off&apos;). </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
