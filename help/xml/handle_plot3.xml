<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="handle_plot3" kind="page">
    <compoundname>handle_plot3</compoundname>
    <title>PLOT3 Plot 3D Function</title>
    <detaileddescription>
<para>Section: <ref refid="sec_handle_1sec_handle" kindref="member">Handle-Based Graphics</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>This is the 3D plot command. The general syntax for its use is <verbatim>  plot3(X,Y,Z,{linespec 1},X,Y,Z,{linespec 2},...,properties...)
</verbatim> where <computeroutput>X</computeroutput> <computeroutput>Y</computeroutput> and <computeroutput>Z</computeroutput> are the coordinates of the points on the 3D line. Note that in general, all three should be vectors. If some or all of the quantities are matrices, then FreeMat will attempt to expand the vector arguments to the same size, and then generate multiple plots, one for each column of the matrices. The linespec is optional, see <computeroutput>plot</computeroutput> for details. You can specify <computeroutput>properties</computeroutput> for the generated line plots. You can also specify a handle as an axes to target <verbatim>  plot3(handle,...)
</verbatim> </para></sect1>
<sect1 id="variables_struct_1Example">
<title>Example</title>
<para>Here is a simple example of a 3D helix.</para><para><verbatim>--&gt; t = linspace(0,5*pi,200);
--&gt; x = cos(t); y = sin(t); z = t;
--&gt; plot3(x,y,z);
--&gt; view(3);
</verbatim></para><para>Shown here <image type="html" name="plt3.png"></image>
 <image type="latex" name="plt3" width="12cm">plt3</image>
 </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
