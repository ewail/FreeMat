<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="array_rref" kind="page">
    <compoundname>array_rref</compoundname>
    <title>RREF Reduced Row Echelon Form of a Matrix</title>
    <detaileddescription>
<para>Section: <ref refid="sec_array_1sec_array" kindref="member">Array Generation and Manipulations</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Calculates the reduced row echelon form of a matrix using Gauss Jordan elimination with partial pivoting. The generic syntax for <computeroutput>rref</computeroutput> is <verbatim>   R = rref(A)
</verbatim> A default tolerance of <computeroutput>max(size(A))*eps*norm(A,inf)</computeroutput> is used to detect negligible column elements. The second form of <computeroutput>rref</computeroutput> returns a vector <computeroutput>k</computeroutput> as well as <computeroutput>R</computeroutput> <verbatim>  [R,k] = rref(A)
</verbatim> where <computeroutput>k</computeroutput> is a vector that correponds to the columns of <computeroutput>A</computeroutput> used as pivot columns. If you want to control the tolerance used to identify negligible elements, you can use the form <verbatim>  [R,k] = rref(A, tolerance)
</verbatim> This implementation of <computeroutput>rref</computeroutput> is based on the one from the matcompat lib for octave. It is copyright Paul Kienzle, and distributed under the GNU GPL. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
