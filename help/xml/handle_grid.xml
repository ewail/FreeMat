<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="handle_grid" kind="page">
    <compoundname>handle_grid</compoundname>
    <title>GRID Plot Grid Toggle Function</title>
    <detaileddescription>
<para>Section: <ref refid="sec_handle_1sec_handle" kindref="member">Handle-Based Graphics</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Toggles the drawing of grid lines on the currently active plot. The general syntax for its use is <verbatim>   grid(state)
</verbatim> where <computeroutput>state</computeroutput> is either <verbatim>   grid(&apos;on&apos;)
</verbatim> to activate the grid lines, or <verbatim>   grid(&apos;off&apos;)
</verbatim> to deactivate the grid lines. If you specify no argument then <computeroutput>grid</computeroutput> toggles the state of the grid: <verbatim>   grid
</verbatim> You can also specify a particular axis to the grid command <verbatim>   grid(handle,...)
</verbatim> where <computeroutput>handle</computeroutput> is the handle for a particular axis. </para></sect1>
<sect1 id="variables_struct_1Example">
<title>Example</title>
<para>Here is a simple plot without grid lines.</para><para><verbatim>--&gt; x = linspace(-1,1);
--&gt; y = cos(3*pi*x);
--&gt; plot(x,y,&apos;r-&apos;);
</verbatim></para><para><image type="html" name="grid1.png"></image>
 <image type="latex" name="grid1" width="12cm">grid1</image>
</para><para>Next, we activate the grid lines.</para><para><verbatim>--&gt; plot(x,y,&apos;r-&apos;);
--&gt; grid on
</verbatim></para><para><image type="html" name="grid2.png"></image>
 <image type="latex" name="grid2" width="12cm">grid2</image>
 </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
