<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="external_ctypenew" kind="page">
    <compoundname>external_ctypenew</compoundname>
    <title>CTYPENEW Create New Instance of C Structure</title>
    <detaileddescription>
<para>Section: <ref refid="sec_external_1sec_external" kindref="member">FreeMat External Interface</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>The <computeroutput>ctypenew</computeroutput> function is a convenience function for creating a FreeMat structure that corresponds to a C structure. The entire structure is initialized with zeros. This has some negative implications, because if the structure definition uses <computeroutput>cenums</computeroutput>, they may come out as <computeroutput>&apos;unknown&apos;</computeroutput> values if there are no enumerations corresponding to zero. The use of the function is <verbatim>   a = ctypenew(&apos;typename&apos;)
</verbatim> which creates a single structure of C structure type <computeroutput>&apos;typename&apos;</computeroutput>. To create an array of structures, we can provide a second argument <verbatim>   a = ctypenew(&apos;typename&apos;,count)
</verbatim> where <computeroutput>count</computeroutput> is the number of elements in the structure array. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
