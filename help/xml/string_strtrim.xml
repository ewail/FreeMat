<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="string_strtrim" kind="page">
    <compoundname>string_strtrim</compoundname>
    <title>STRTRIM Trim Spaces from a String</title>
    <detaileddescription>
<para>Section: <ref refid="sec_string_1sec_string" kindref="member">String Functions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>Removes the white-spaces at the beginning and end of a string (or a cell array of strings). See <computeroutput>isspace</computeroutput> for a definition of a white-space. There are two forms for the <computeroutput>strtrim</computeroutput> function. The first is for single strings <verbatim>   y = strtrim(strng)
</verbatim> where <computeroutput>strng</computeroutput> is a string. The second form operates on a cell array of strings <verbatim>   y = strtrim(cellstr)
</verbatim> and trims each string in the cell array. </para></sect1>
<sect1 id="variables_struct_1Example">
<title>Example</title>
<para>Here we apply <computeroutput>strtrim</computeroutput> to a simple string</para><para><verbatim>--&gt; strtrim(&apos;  lot of blank spaces    &apos;);
</verbatim></para><para>and here we apply it to a cell array</para><para><verbatim>--&gt; strtrim({&apos;  space&apos;,&apos;enough &apos;,&apos; for &apos;,&apos;&apos;})

ans = 
 [space] [enough] [for] [] 
</verbatim> </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
