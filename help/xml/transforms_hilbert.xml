<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="transforms_hilbert" kind="page">
    <compoundname>transforms_hilbert</compoundname>
    <title>HILBERT Hilbert Transform</title>
    <detaileddescription>
<para>Section: <ref refid="sec_transforms_1sec_transforms" kindref="member">Transforms/Decompositions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>The <computeroutput>hilbert</computeroutput> function computes the hilbert transform of the argument vector or matrix. The FreeMat <computeroutput>hilbert</computeroutput> function is compatible with the one from the MATLAB API. This means that the output of the <computeroutput>hilbert</computeroutput> function is the sum of the original function and an imaginary signal containing the Hilbert transform of it. There are two syntaxes for the hilbert function. The first is <verbatim>  y = hilbert(x)
</verbatim> where <computeroutput>x</computeroutput> is real vector or matrix. If <computeroutput>x</computeroutput> is a matrix, then he Hilbert transform is computed along the columns of <computeroutput>x</computeroutput>. The second syntax provides a dimension along which to take the transform. <verbatim>  y = hilbert(x,n)
</verbatim> where <computeroutput>n</computeroutput> is the dimension along which to apply the transformation. </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
