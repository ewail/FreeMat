<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="external_ctypesize" kind="page">
    <compoundname>external_ctypesize</compoundname>
    <title>CTYPESIZE Compute Size of C Struct</title>
    <detaileddescription>
<para>Section: <ref refid="sec_external_1sec_external" kindref="member">FreeMat External Interface</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>The <computeroutput>ctypesize</computeroutput> function is used to compute the size of a C structure that is defined using the <computeroutput>ctypedefine</computeroutput> function. The usage of <computeroutput>ctypesize</computeroutput> is <verbatim>   size = ctypesize(&apos;typename&apos;)
</verbatim> where <computeroutput>typename</computeroutput> is the name of the C structure you want to compute the size of. The returned count is measured in bytes. Note that as indicated in the help for <computeroutput>ctypedefine</computeroutput> that FreeMat does not automatically pad the entries of the structure to match the particulars of your C compiler. Instead, the assumption is that you have adequate padding entries in your structure to align the FreeMat members with the C ones. See <computeroutput>ctypedefine</computeroutput> for more details. You can also specify an optional count parameter if you want to determine how large multiple structures are <verbatim>   size = ctypesize(&apos;typename&apos;,count)
</verbatim> </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
