<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.1.1">
  <compounddef id="string_deblank" kind="page">
    <compoundname>string_deblank</compoundname>
    <title>DEBLANK Remove trailing blanks from a string</title>
    <detaileddescription>
<para>Section: <ref refid="sec_string_1sec_string" kindref="member">String Functions</ref> </para><sect1 id="vtkwidgets_vtkxyplotwidget_1Usage">
<title>Usage</title>
<para>The <computeroutput>deblank</computeroutput> function removes spaces at the end of a string when used with the syntax <verbatim>   y = deblank(x)
</verbatim> where <computeroutput>x</computeroutput> is a string, in which case, all of the extra spaces in <computeroutput>x</computeroutput> are stripped from the end of the string. Alternately, you can call <computeroutput>deblank</computeroutput> with a cell array of strings <verbatim>   y = deblank(c)
</verbatim> in which case each string in the cell array is deblanked. </para></sect1>
<sect1 id="variables_struct_1Example">
<title>Example</title>
<para>A simple example</para><para><verbatim>--&gt; deblank(&apos;hello   &apos;)

ans = 
hello
</verbatim></para><para>and a more complex example with a cell array of strings</para><para><verbatim>--&gt; deblank({&apos;hello  &apos;,&apos;there &apos;,&apos;  is  &apos;,&apos;  sign  &apos;})

ans = 
 [hello] [there] [  is] [  sign] 
</verbatim> </para></sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
