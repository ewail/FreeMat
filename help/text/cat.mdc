
 Usage

This function concatenates arrays in a given dimension. The
syntax for its use is

     cat (DIM, A, B)
     cat (DIM, A, B, C ...)

to return the concatenation along the dimension DIM of all
arguments. cat(1, A, B, C) is the same as [A; B; C] or
vertcat(A, B, C). cat(2, A, B, C) is the same as [A, B, C]
or horzcat(A, B, C).

* FreeMat_Documentation
* Object_Oriented_Programming
* Generated on Thu Jul 25 2013 17:17:13 for FreeMat by
  doxygen_ 1.8.1.1

